// config.js — Configuración global del proyecto
const CONFIG = {
    // Cuando deproyes, cambia esta URL por la de tu backend (ej: https://tu-backend.railway.app)
    API_URL: 'http://localhost:3000'
};
